# Roadmap

The project should stay small and technician-friendly. New features should reduce real label-printing friction instead of turning the app into a heavy design suite.

## Done foundation

- Public README
- MIT license
- Python package structure
- Core tests
- GUI launcher
- CLI ZPL generation
- Clean shutdown behavior
- Safe per-user settings storage
- Menu-based advanced options
- Multi-line label text editor
- `.zpl` export/import for labels generated by this tool
- Built-in label presets
- Text cleanup tools
- Batch ZPL generation from pasted text blocks

## Near-term usability

- Add real screenshots from Windows
- Improve visual spacing and iconography after real GUI review
- Add packaged Windows release build
- Add QR code support
- Add network printer backend for TCP port 9100
- Add printer diagnostics before sending RAW data

## Batch workflows

- CSV import
- Batch preview before print/export
- Batch print safeguards and confirmation summary
- Template variables such as `{serial}`, `{date}`, `{row}`
- Table-style editor for batch labels

## Printing backends

- Backend selection in the GUI
- TCP/IP printer support
- Optional dry-run printer checks
- Clearer printer error diagnostics

## Later scope

- More barcode types
- Template sharing
- Better preview/rendering options
- Cross-platform print backends if they can stay cleanly separated from the core
